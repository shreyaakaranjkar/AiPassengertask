import { CountDirective } from './count.directive';

describe('CountDirective', () => {
  it('should create an instance', () => {
    const directive = new CountDirective();
    expect(directive).toBeTruthy();
  });
});
