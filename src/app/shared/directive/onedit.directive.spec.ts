import { OneditDirective } from './onedit.directive';

describe('OneditDirective', () => {
  it('should create an instance', () => {
    const directive = new OneditDirective();
    expect(directive).toBeTruthy();
  });
});
